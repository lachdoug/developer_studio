<div class="dropdown pull-right">
  <button class="btn btn-custom dropdown-toggle" type="button" data-toggle="dropdown">
    <%= icon 'fa-bars' %>
  </button>
  <ul class="dropdown-menu">
    <li><%= link_to icon_text('fa-git-square', 'Repository'), service_repository_path(service_id: @service.name) %></li>
    <li role="separator" class="divider"></li>
    <li>
      <%= link_to icon_text('fa-trash', :Delete),
          service_path(service_id: @service.name),
          method: :delete,
          data: {confirm: "Are you sure that you want to delete #{@service.name}?"} %>
    </li>
  </ul>
</div>
